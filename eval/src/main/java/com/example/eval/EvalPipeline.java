package com.example.eval;

public class EvalPipeline {
    // takes in data, runs the pipeline, and returns the result
    // for each of the saved accessibility event & UISnapshot, run the graph query, get the result, compare and calculate F1 score
}
